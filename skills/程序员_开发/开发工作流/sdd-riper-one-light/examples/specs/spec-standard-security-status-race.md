# Spec: 协作资源安全状态竞争修复（公开示例）

## Goal
- 要解决什么问题：
  某类协作资源在异步安全检测过程中出现状态竞争，先写入了偏乐观的中间状态，随后又被后续回调修正为更严格的最终状态，导致客户端可能短暂读取到错误结果。
- 验收结果：
  同一资源在该场景下不再先暴露错误中间状态，再被后续异步流程纠正；对外只呈现最终可信状态。

## Scope
- In:
  安全状态管理模块
  状态回写处理模块
  资源元数据更新模块
- Out:
  客户端改造
  下载链路策略改造
  与当前问题无关的规则扩展

## Facts / Constraints
- 已确认事实：
  同一资源在一次问题样本中经历了“早期乐观状态”与“后续严格状态”的冲突回写。
  最终结果是严格状态正确，但问题在于中间过程对外暴露了错误信号。
  当前系统同时存在旧兼容链路与新检测链路，两者叠加后形成状态竞争。
  同一次状态更新存在重复写回，放大了问题表现。
- 技术/业务约束：
  不改变当前对外状态定义。
  不粗暴删除历史兼容逻辑，避免影响仍依赖旧链路的场景。
  代码修改前需先获得明确批准。
- 已知风险：
  如果只去掉重复写回，但不处理状态来源冲突，问题仍会存在。
  如果直接删除兼容分支，可能影响历史路径稳定性。

## Open Questions
- [ ] 新检测链路是否已经完全摆脱旧兼容结果依赖。
- [ ] 是否需要增加状态机保护，避免更严格状态被后续降级覆盖。

## Restated Understanding
- 我理解当前任务是：
  修复一个典型的异步状态竞争问题，避免资源对外短暂暴露错误安全状态。
- 当前核心目标是：
  让状态更新只沿着可信方向收敛，并消除重复写回。
- 当前边界是：
  只处理服务端上传阶段的状态竞争，不改客户端，不改其他链路策略。
- 暂不处理：
  状态定义扩展、客户端消费逻辑调整、无关模块重构。

## Checkpoint Summary
- 当前任务理解：
  这是旧兼容逻辑与新检测链路叠加产生的状态竞争。
- 当前核心目标：
  消除早到的错误状态，并收敛单次更新的重复写回。
- 当前进度：
  已通过样本日志与代码阅读确认现象、来源链路与重复更新位置。
- 下一步 1:
  解耦新检测链路与旧兼容链路，避免提前回写错误中间状态。
- 下一步 2:
  评估是否需要增加状态机保护，禁止检测过程中发生逆向状态回退。
- 下一步 3:
  收敛重复写回，保留单一可信更新入口。
- 涉及文件 / 模块：
  安全状态管理、状态处理、资源元数据更新
- 风险：
  不能把真正仍在服务的兼容路径一起改坏。
- 验证方式：
  复现实验场景，确认不再出现“错误中间状态 -> 最终严格状态”的对外冲突。
- Execution Approval: `Approved`

## Change Log
- 2026-03-10: 建立 micro-spec，沉淀问题现象、根因假设和修复方向。
- 2026-03-10: 切断新检测链路对旧兼容路径的错误复用，避免提前回写错误状态。
- 2026-03-10: 移除默认场景中的重复直接回写，仅保留统一状态更新入口。
- 2026-03-10: 调整对应测试用例，使兼容分支与默认分支边界更清晰。

## Validation
- 已执行检查：
  对比多份问题日志样本。
  阅读相关状态管理、处理与元数据更新实现。
  复核本次代码 diff，确认修改范围聚焦于状态竞争与重复写回。
  执行相关测试，覆盖状态回写与最终结果校验场景。
- 结果：
  已确认问题现象与代码路径一致。
  已完成最小修复：新检测链路不再提前写入错误中间状态。
  已完成重复写回修复：默认场景单次状态更新只保留一条可信写回。
  相关测试通过。
- 剩余风险：
  客户端是否对历史错误状态存在本地缓存行为，仍需结合真实终端继续确认。
